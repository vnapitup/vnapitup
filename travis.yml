language: python
python:
  - "3.7"
sudo: required
branches:
  only:
    - sources
  env:
    global:
install:
- pip install ghp-import
- git clone https://github.com/getpelican/pelican-plugins
script:
- make publish github
